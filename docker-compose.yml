version: "3.9"

services:
  bank-app:
    container_name: bank-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    restart: unless-stopped